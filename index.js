// const text= prompt(" write something here to change it to UPPERCASE : ");
// const result = text.toUpperCase();

// console.log(result);

function toupper() {
    const text = prompt(" write something here to change it to UPPERCASE :");
    const result = text.toUpperCase();
    return(result);
}

console.log(toupper());